# Copy this file to .env and set values before running the Flask server.
# The app reads these at startup; .env itself is gitignored.

# Flask session secret key.
# If unset, a random key is generated on each startup (sessions won't survive restarts).
# Generate a good one with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=
